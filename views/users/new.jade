extends ../layout

block content
  h1 Add Feature to #{user}
  form(action="/rollout/users/#{user}/features", method="POST")
    input(type="text", name="feature", placeholder="Name")
    button(type="submit") Add